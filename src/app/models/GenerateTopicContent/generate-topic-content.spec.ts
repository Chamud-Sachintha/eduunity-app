import { GenerateTopicContent } from './generate-topic-content';

describe('GenerateTopicContent', () => {
  it('should create an instance', () => {
    expect(new GenerateTopicContent()).toBeTruthy();
  });
});
